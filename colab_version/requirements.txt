# Full set (used by the in-cell widget editor + the legacy notebook).
# backend_colab.ipynb installs only what's missing — see DEPS in
# colab_launch.py (numpy, opencv, mediapipe, faster-whisper).
numpy
opencv-python
mediapipe
openai-whisper
faster-whisper
ffmpeg-python>=0.2.0
moviepy>=2.0.0
pydub
ipywidgets>=8.0.0
